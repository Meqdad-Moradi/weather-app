import { BaseEntry } from './base-entry';

describe('BaseEntry', () => {
  it('should create an instance', () => {
    const directive = new BaseEntry();
    expect(directive).toBeTruthy();
  });
});
