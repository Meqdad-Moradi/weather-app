import { TogglePasswordVisibility } from './toggle-password-visibility';

describe('TogglePasswordVisibility', () => {
  it('should create an instance', () => {
    const directive = new TogglePasswordVisibility();
    expect(directive).toBeTruthy();
  });
});
